{
  "name": "smart-desk-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hebrew AI study assistant powered by Teachable Machine and Gemini",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local server.mjs",
    "start": "node --env-file-if-exists=.env.local server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=22" }
}
